"""Experiment automation for FinP.

Submodules:
    config   - per-dataset experiment matrices (commands for ``main_fed.py``)
    run      - run all experiments for a dataset and tee logs to disk
    compare  - parse the ``RUN SUMMARY`` block from logs into a comparison table
"""
